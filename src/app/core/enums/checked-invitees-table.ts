export enum CheckedInviteesTable {
  VISITOR_NAME,
  SCANNED,
  TABLE_NUMBER,
  VERIFIED_BY,
}

export enum CheckedInviteesTableKeys {
  VISITOR_NAME = 'visitor_name',
  SCANNED = 'scan_status',
  TABLE_NUMBER = 'table_number',
  VERIFIED_BY = 'scanned_by',
}
